---
title: ToKoDraw | Modes
description: The three ToKoDraw painting modes in Blender: 3D handpainting with texture paint, CamView (2D camera view on a 3D object) and Canvas 2D mode, plus navigation shortcuts.
---

# ToKoDraw Painting Modes & Workflows

ToKoDraw offers three complementary **painting modes in Blender**: painting directly on a 3D object, painting a 3D object through a 2D camera view (CamView), and a full **2D canvas mode**. Each mode creates the same non-destructive layer system, so you can switch between them freely.

## Mode 1 — Handpainting a 3D Object

Select your mesh, then click Create Material to apply it to the 3D object.
The TKD material is automatically created, and Texture Paint mode is activated with an Emission shader configured for painting. The Brush & Color Settings panel opens immediately to prepare the tool. Two layers are generated by default:

**Canvas** — the background layer. It cannot be deleted, but it can be hidden to make the object fully transparent when using the emission shader.

**Layer 1** — the first painting layer, ready to use right away.

![Creating a material on a 3D object in ToKoDraw Mode 1](/assets/tuto/modes/3D/3D.png)

![The TKD material with emission shader and default layers](/assets/tuto/modes/3D/Mat.png)


### CamView mode — paint a 3D object through a 2D camera view

CamView adds a dedicated panel that lets you paint a 3D object through a **2D camera view**, while keeping full control over the object in Texture Paint. This hybrid workflow is particularly effective for touch-ups, fine details and perspective adjustments.

- 2D camera view for painting directly on a 3D object
- Full control of the object in Texture Paint
- Movement along the X / Y / Z axes
- Object rotation
   Action | Shortcut |
 |---|---|
 | Zoom | Ctrl + middle-mouse click |
 | Pan | Shift + middle-mouse click |
 | Exit camera view | Middle-mouse click or camera icon |
 | Return to camera view | Numpad 0 or camera icon |

![The CamView panel in ToKoDraw](/assets/tuto/modes/3D/camview.png)

1 - *Switch CamView on/off*

2 - *Show or hide the Transform panel*

3 - *Transform panel*

![Moving a 3D object while painting in CamView mode](/assets/tuto/modes/3D/move3Dobj.gif)



## Mode 2 — Canvas 2D mode

In this mode, ToKoDraw transitions from 3D handpainting to a dedicated 2D painting workflow. After selecting an object and choosing Create Canvas, the user can decide whether the selected mesh should be hidden or removed, and define both the canvas size and its orientation (vertical or horizontal). Once validated, the addon generates a new plane as the 2D surface, creates a dedicated 2D camera, and automatically switches the viewport to camera view, allowing the user to paint just like in a traditional 2D software.

![The Create Canvas options in ToKoDraw 2D mode](/assets/tuto/modes/2D/2D.png)

![Choosing canvas size and orientation](/assets/tuto/modes/2D/size.png)

As in Mode 1, the addon automatically creates the TKD material using an Emission shader configured for painting. Two layers are generated by default:

**Canvas** — the background layer, non‑deletable but hideable to make the plane fully transparent.

**Layer 1** — the first active painting layer, ready to use immediately.

![The default TKD layers created in Canvas 2D mode](/assets/tuto/modes/2D/canvas.png)


### Canvas navigation shortcuts

 **Action & Shortcut**


 Zoom - Ctrl + middle-mouse click 

 Pan - Shift + middle-mouse click |

![Zooming on the 2D canvas](/assets/tuto/modes/2D/zoom.gif)

![Panning the 2D canvas](/assets/tuto/modes/2D/move.gif)

The canvas can also be oriented and adjusted through the **Transform panel**, inside the ToKoDraw panel. Movements are performed along the axes:

- **X** — lateral movement
- **Y** — depth (forward / backward)
- **Z** — height (up / down)

![Moving the canvas along the X axis](/assets/tuto/modes/2D/xmove.gif)


![Moving the canvas along the Y axis](/assets/tuto/modes/2D/ymove.gif)


![Moving the canvas along the Z axis](/assets/tuto/modes/2D/zmove.gif)


…as well as **rotation**, for precise orientation of the painting surface.

![Rotating the canvas on the Y axis](/assets/tuto/modes/2D/yrotate.gif)

![Rotating the canvas on the X and Z axes](/assets/tuto/modes/2D/xzrotate.gif)

## Next steps

- Learn the layer system: opacity, masks, merge, normal layers
- Install ToKoDraw if you haven't yet
<a class="download-btn" href="https://tomkohai.github.io/ToKoDraw/download.html" target="_blank"> 
    Download Page
  </a>

## FAQ

### Can I paint in 2D inside Blender with ToKoDraw?
Yes — Canvas 2D mode creates a plane, a dedicated 2D camera and a Krita-like painting environment inside Blender's viewport.

### What is CamView mode?
CamView lets you paint a 3D object through a 2D camera view while keeping full control over the object in Texture Paint — useful for touch-ups and perspective adjustments.

### Do all modes share the same layer system?
Yes, every mode creates the same non-destructive node-group layer stack.